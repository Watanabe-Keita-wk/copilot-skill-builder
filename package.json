{
  "name": "copilot-skill-builder",
  "version": "0.1.0",
  "private": true,
  "description": "AI Coding Learning Platform with GitHub Copilot",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build:web": "cd apps/web && pnpm build",
    "build:extension": "cd apps/vscode-extension && pnpm build",
    "db:prepare": "cd apps/web && npx prisma generate && (npx prisma migrate deploy || npx prisma db push) && (npx prisma db seed || true)",
    "build": "pnpm run db:prepare && pnpm run build:web && pnpm run build:extension",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "db:migrate": "pnpm --filter web db:migrate",
    "db:seed": "pnpm --filter web db:seed",
    "db:studio": "pnpm --filter web db:studio"
  },
  "keywords": [
    "copilot",
    "learning",
    "vscode",
    "education"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
